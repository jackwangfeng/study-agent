project_name: study-agent-backend
version: 0.1.0
port: 8000
debug: true
# .env 里的 DATABASE_URL / QWEN_API_KEY / LLM_MODEL 会覆盖这里的 fallback。
database_url: postgresql://postgres:postgres@localhost:5434/study_agent?sslmode=disable
redis_url: redis://localhost:6379/0
secret_key: your-secret-key-change-in-production
log_level: debug
llm_api_url: https://dashscope.aliyuncs.com/compatible-mode/v1
llm_model: qwen3.5-omni-plus
